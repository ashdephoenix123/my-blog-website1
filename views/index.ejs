<%- include('partials/header') -%>

    <h2>Blogs available</h2>
    <p> 
      <%  paraOne.forEach((post)=> { %>
        <h3><%= post.title %></h3>
        <p><%= (post.description).substring(0,120) + "..." %> <a href="/posts/<%= post._id %>">read more</a></p>
      <%  })  %>
    </p>

    <%- include('partials/footer') -%>
